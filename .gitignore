# PyInstaller build artefacts — do not commit
build/
dist/
*.egg-info/
__pycache__/
*.pyc

# Cython-compiled binaries — platform-specific, not committed
*.so
*.c

# npm postinstall drops the platform binary here — never commit
native/
package-lock.json
